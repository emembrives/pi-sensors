stream
  |from()
    .database('telegraf')
    .retentionPolicy('high_definition')
  |window()
    .period(10m)
    .every(10m)
    .align()
  |influxDBOut()
    .database('telegraf')
    .retentionPolicy('low_definition')
    .precision('m')
